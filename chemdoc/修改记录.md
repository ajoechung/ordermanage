# 修改记录

## 2026-05-28

### 09:xx:xx - 修复数据隔离功能bug（第27次修复）
**修改文件**:
- [backend/app/service/DataScopeService.php](file:///workspace/chemdoc/backend/app/service/DataScopeService.php)
- [backend/app/service/StatisticsService.php](file:///workspace/chemdoc/backend/app/service/StatisticsService.php)

**修改内容**:
1. DataScopeService.php - 修复 `applyCustomerScope()` 和 `applySupplierScope()` 方法
   - 如果用户不是管理员且没有负责的数据，添加 `where('customer_id', 0)` 条件返回空结果
   - 防止业务员用户看到所有数据
2. StatisticsService.php - 在 `getDashboard()`、`getCustomerStats()` 和 `getOrderStats()` 方法中添加相同的权限检查逻辑
3. 修复效果：业务员（非管理员）登录后只能看到自己负责的数据，如果没有负责任何数据则列表显示为空

---

### 04:xx:xx - 修复角色分配接口500错误（第26次修复）
**提交**: `1fbccc97`
**修改文件**:
- [backend/app/controller/api/System.php](file:///workspace/chemdoc/backend/app/controller/api/System.php)

**修改内容**:
1. 添加 try-catch 异常处理，捕获并记录错误信息
2. 改进请求数据解析，支持 JSON 和表单两种格式
3. 添加数据类型检查和过滤，确保 group_ids 是数组且包含有效ID
4. 记录详细的错误日志便于排查

---

### 04:xx:xx - 修复用户列表角色显示问题（第25次修复）
**提交**: `3c310500`
**修改文件**:
- [backend/app/controller/api/System.php](file:///workspace/chemdoc/backend/app/controller/api/System.php)

**修改内容**:
1. 改进角色数据处理逻辑，添加更严格的类型检查
2. 确保 `auth_group_access` 数组正确遍历
3. 使用空值合并运算符处理可能的空值情况
4. 确保 `groups` 字段正确返回给前端

---

### 04:xx:xx - 登录页面和顶部导航优化（第24次修复）
**修改文件**:
- [frontend/src/views/login/index.vue](file:///workspace/chemdoc/frontend/src/views/login/index.vue)
- [frontend/src/components/layout/index.vue](file:///workspace/chemdoc/frontend/src/components/layout/index.vue)

**修改内容**:
1. 登录页面标题修改："化工单据管理系统" → "泰锋化工单据管理系统"
2. 英文标题修改："Chemical Document Management System" → "TaiFeng Chemical Document Management System"
3. 移除登录表单的默认值（用户名、密码不再预填充）
4. 移除底部"演示账号：admin / 123456"提示
5. 顶部导航移除 logo 图标
6. 顶部导航标题修改："化工单据" → "泰锋化工"

---

### 03:xx:xx - 订单列表新增再来一单和去采购功能（第23次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)
- [frontend/src/views/purchase/list.vue](file:///workspace/chemdoc/frontend/src/views/purchase/list.vue)

**修改内容**:
1. 订单列表操作栏新增"再来一单"按钮，点击后打开新增订单弹窗并填充原订单数据
2. 订单列表操作栏新增"去采购"按钮，点击后跳转到采购单页面，自动打开新增采购单弹窗并填充产品数据，关联原订单编号
3. 采购单页面添加路由参数处理逻辑，支持从订单页面接收数据并自动填充表单

---

### 03:xx:xx - 调整订单和采购单单价、数量输入框长度（第22次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)
- [frontend/src/views/purchase/list.vue](file:///workspace/chemdoc/frontend/src/views/purchase/list.vue)

**修改内容**:
1. 单价列宽度：120px → 100px，输入框宽度：100% → 80px
2. 数量列宽度：150px → 160px，输入框宽度：80px → 100px

---

### 02:xx:xx - 修复订单和采购单产品单位保存和显示问题（第21次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)
- [backend/app/service/PurchaseService.php](file:///workspace/chemdoc/backend/app/service/PurchaseService.php)
- [database/migration_20260528_add_product_unit.sql](file:///workspace/chemdoc/database/migration_20260528_add_product_unit.sql)

**修改内容**:
1. 前端：handleSubmit添加product_unit、product_name、product_spec、subtotal字段提交
2. 后端：PurchaseService getDetail方法添加spec、price、amount字段映射
3. 新增数据库迁移文件

---

### 01:xx:xx - 修复采购单产品单位显示问题（第20次修复）
**修改文件**:
- [backend/app/service/PurchaseService.php](file:///workspace/chemdoc/backend/app/service/PurchaseService.php)

**修改内容**:
1. getDetail方法添加product_unit字段返回
2. create方法添加product_unit字段保存
3. update方法添加product_unit字段保存

---

### 00:xx:xx - 修复订单产品单位显示问题（第19次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)
- [backend/app/service/OrderService.php](file:///workspace/chemdoc/backend/app/service/OrderService.php)

**修改内容**:
1. 前端：handleProductChange函数添加product_unit字段获取，addItem函数添加product_unit初始化，数量列显示单位
2. 后端：getDetail方法添加product_unit字段返回

---

## 2026-05-27

### 22:xx:xx - 修复订单详情和编辑订单的产品单位显示（第18次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)

**修改内容**:
1. 订单详情弹窗的订单明细表格中，数量列添加了产品单位显示
2. handleEdit 函数中处理 items 时添加了 product_unit 字段

---

### 21:xx:xx - 修复产品单位显示问题（第17次修复）
**修改文件**:
- [backend/app/service/OrderService.php](file:///workspace/chemdoc/backend/app/service/OrderService.php)
- [backend/app/service/PurchaseService.php](file:///workspace/chemdoc/backend/app/service/PurchaseService.php)

**修改内容**:
1. OrderService.php - getDetail方法中添加了product_unit字段的返回，优先从订单明细表读取，否则从产品表获取
2. PurchaseService.php - getDetail方法中添加了product_unit字段的返回
3. PurchaseService.php - create和update方法中添加了product_unit字段的保存

---

### 20:xx:xx - 订单和采购单详情添加产品单位显示（第16次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)
- [frontend/src/views/purchase/list.vue](file:///workspace/chemdoc/frontend/src/views/purchase/list.vue)

**修改内容**:
1. 订单详情弹窗的订单明细表格中，在"数量"列添加产品单位显示
2. 采购单详情弹窗的采购明细表格中，在"数量"列添加产品单位显示

---

### 19:xx:xx - 采购单表单产品表格添加单位显示（第15次修复）
**修改文件**:
- [frontend/src/views/purchase/list.vue](file:///workspace/chemdoc/frontend/src/views/purchase/list.vue)

**修改内容**:
1. 在采购产品表格的"数量"列中添加产品单位显示，放在数量后面
2. 调整各列宽度：产品列 min-width: 200→220，规格列 width: 100→120，数量列 width: 140→150，金额列 width: 100→120
3. 在 `handleProductChange` 函数中添加 `product_unit` 字段获取
4. 在 `addItem` 函数中添加 `product_unit` 字段
5. 在 `handleEdit` 函数中处理 `product_unit` 字段

---

### 18:xx:xx - 订单表单产品表格添加单位显示（第14次修复）
**修改文件**:
- [frontend/src/views/order/list.vue](file:///workspace/chemdoc/frontend/src/views/order/list.vue)

**修改内容**:
1. 在订单产品表格的"数量"列中添加产品单位显示，放在数量后面
2. 调整各列宽度：产品列 min-width: 200→220，规格列 width: 100→120，数量列 width: 140→150，金额列 width: 100→120
3. 在 `handleProductChange` 函数中添加 `product_unit` 字段获取
4. 在 `addItem` 函数中添加 `product_unit` 字段
5. 在 `handleEdit` 函数中处理 `product_unit` 字段

---

### 17:xx:xx - 修复跟进记录下次跟进字段和联系人主联系人显示问题（第13次修复）
**提交**: `63d0198a`
**修改文件**:
- [frontend/src/views/customer/follow.vue](file:///workspace/chemdoc/frontend/src/views/customer/follow.vue)
- [frontend/src/views/customer/contact.vue](file:///workspace/chemdoc/frontend/src/views/customer/contact.vue)

**修改内容**:
1. follow.vue - 下次跟进字段 `next_follow_time` 已在列表和表单中正确使用
2. contact.vue - 编辑联系人时，对 `is_default` 字段添加 `Boolean()` 类型转换，确保 el-switch 正确显示状态

---

### 16:xx:xx - 修复跟进记录和联系人列表500错误（第12次修复）
**修改文件**:
- [backend/app/service/ContactService.php](file:///workspace/chemdoc/backend/app/service/ContactService.php)
- [backend/app/service/FollowService.php](file:///workspace/chemdoc/backend/app/service/FollowService.php)

**修改内容**:
1. 移除 `createUser` 关联查询，避免关联查询导致的500错误
2. `create_user_name` 直接从数据库表字段中读取，不再使用关联查询
3. 保留 `customer` 和 `followUser` 关联查询

---

### 16:xx:xx - 修复跟进记录500错误和创建人保存问题（第11次修复）
**修改文件**:
- [backend/app/model/CustomerFollowModel.php](file:///workspace/chemdoc/backend/app/model/CustomerFollowModel.php)
- [backend/app/service/ContactService.php](file:///workspace/chemdoc/backend/app/service/ContactService.php)
- [backend/app/service/FollowService.php](file:///workspace/chemdoc/backend/app/service/FollowService.php)
- [database/migration_20260527_add_create_user_name.sql](file:///workspace/chemdoc/database/migration_20260527_add_create_user_name.sql) - 新增

**修改内容**:
1. CustomerFollowModel - 添加缺失的 `AdminUserModel` 和 `CustomerModel` 导入，修复关联查询失败
2. ContactService - 在 create 方法中添加 `create_user_name` 字段保存
3. FollowService - 在 create 方法中添加 `create_user_id` 和 `create_user_name` 字段保存
4. 创建数据库迁移文件，为 `contact` 和 `customer_follow` 表添加 `create_user_name` 字段

---

### 16:xx:xx - 联系人、跟进记录添加创建人功能（第10次修复）
**修改文件**:
- [backend/app/service/ContactService.php](file:///workspace/chemdoc/backend/app/service/ContactService.php)
- [backend/app/service/FollowService.php](file:///workspace/chemdoc/backend/app/service/FollowService.php)
- [frontend/src/views/customer/contact.vue](file:///workspace/chemdoc/frontend/src/views/customer/contact.vue)
- [frontend/src/views/customer/follow.vue](file:///workspace/chemdoc/frontend/src/views/customer/follow.vue)

**修改内容**:
1. ContactService - 在 getList 方法中添加 createUser 关联查询，处理 create_user_name 字段
2. FollowService - 在 getList 方法中添加 createUser 关联查询，处理 create_user_name 字段
3. contact.vue - 在联系人列表中添加"创建人"列显示
4. follow.vue - 在跟进记录列表中添加"创建人"列显示
5. 数据库表已包含 create_user_id 字段，无需修改数据库

---

### 16:xx:xx - 修复force参数解析问题（第9次修复）
**修改文件**:
- [backend/app/controller/api/Customer.php](file:///workspace/chemdoc/backend/app/controller/api/Customer.php)

**修改内容**:
- 修复 URL 参数 `force=false` 被错误解析为布尔值 `true` 的问题
- 添加字符串值判断逻辑：只有当字符串是 `"true"`（不区分大小写）时才转换为 `true`
- 这样确保第一次删除时 `force=false` 能正确返回 `code=201`（需要确认）

---

### 15:xx:xx - 修复跟进记录获取失败问题（第8次修复）
**修改文件**:
- [backend/app/service/FollowService.php](file:///workspace/chemdoc/backend/app/service/FollowService.php)
- [backend/app/service/DataScopeService.php](file:///workspace/chemdoc/backend/app/service/DataScopeService.php)

**修改内容**:
- 在 `FollowService::getList` 方法中添加 try-catch 异常处理，捕获并返回错误信息
- 在 `DataScopeService::getAccessibleCustomerIds` 方法中添加用户ID检查和异常处理，避免空值导致的错误

---

### 15:xx:xx - 重新构建前端代码，确保客户删除逻辑正确（第7次修复）
**提交**: `bc12e8a5`
**修改文件**:
- [frontend/src/views/customer/list.vue](file:///workspace/chemdoc/frontend/src/views/customer/list.vue)
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php)

**修改内容**:
- 前端 `handleDelete` 方法：先调用删除接口，检查返回的 code 值
  - 如果 `code === 201`：表示客户存在联系人，弹出确认框，确认后再次调用删除接口并传递 `force=true`
  - 如果 `code === 200`：表示删除成功
- 后端 `delete` 方法：检查联系人数量，如果有联系人且 `force=false`，返回 `code=201` 提示确认；`force=true` 时先删除联系人再删除客户

---

## 2026-05-26

### 17:xx:xx - 修复客户删除和联系人默认标记问题（第6次修复）
**提交**: `2828463e`
**修改文件**:
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php)
- [backend/app/service/ContactService.php](file:///workspace/chemdoc/backend/app/service/ContactService.php)

**修改内容**:
1. 修复客户删除逻辑：检查订单时排除已删除的订单（添加 `whereNull('delete_time')`）
2. 修复联系人默认标记字段：后端使用 `is_default` 替代 `is_primary`，与数据库表字段保持一致

---

### 16:xx:xx - 修复供应商 code 字段唯一键冲突（第5次修复）
**提交**: `3c940f2a`
**修改文件**:
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)

**修改内容**:
1. 在 create 方法中，当 code 为空时设置为 null，避免空字符串导致唯一键冲突
2. 在 update 方法中，单独处理 code 字段，检查编码重复，同样为空时设置为 null
3. update 方法改用直接赋值的方式，避免批量更新的问题

---

### 15:xx:xx - 完善供应商模块（第4次修复）
**提交**: `fa2024dd`
**修改文件**:
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)

**修改内容**:
1. 更新 SupplierService 的 getList 方法，支持前端发送的搜索参数（name, contact, cooperation_status）
2. 在 getList 和 create 方法中添加 try-catch 错误处理，提高代码健壮性
3. 在 create 方法中添加用户ID检查，与客户模块保持一致

---

### 14:50:xx - 修复客户和供应商列表查询问题（第3次修复）
**提交**: `27a386c7`
**修改文件**:
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php)
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)

**修改内容**:
1. 移除 CustomerService 和 SupplierService 中的 scope 调用，直接使用 where 查询，避免 scope 导致的问题
2. 统一字段命名：使用 `owner_user_name` 替代 `owner_name`
3. 优化查询逻辑，提高代码健壮性

---

### 14:30:xx - 修复供应商新增失败问题（第2次修复）
**提交**: `6d44585b`
**修改文件**:
- [database/migrate_add_supplier_fields.sql](file:///workspace/chemdoc/database/migrate_add_supplier_fields.sql) - 新增
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)
- [backend/app/model/SupplierModel.php](file:///workspace/chemdoc/backend/app/model/SupplierModel.php)

**修改内容**:
1. 为 `supplier` 表添加缺失字段：`contact`（联系人）、`phone`（联系电话）、`cooperation_status`（合作状态）、`remark`（备注）
2. 修改 SupplierService 的 create 方法，支持新增字段的保存
3. 修改 SupplierService 的 update 方法，支持新增字段的更新
4. 更新 SupplierModel 的 `fillable` 属性，包含所有新增字段

---

### 14:15:xx - 添加负责人和创建人姓名字段（第1次修复）
**提交**: `58a5416b`
**修改文件**:
- [database/migrate_add_user_name_fields.sql](file:///workspace/chemdoc/database/migrate_add_user_name_fields.sql) - 新增
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php)
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)
- [backend/app/model/CustomerModel.php](file:///workspace/chemdoc/backend/app/model/CustomerModel.php)
- [backend/app/model/SupplierModel.php](file:///workspace/chemdoc/backend/app/model/SupplierModel.php)
- [frontend/src/views/customer/list.vue](file:///workspace/chemdoc/frontend/src/views/customer/list.vue)
- [frontend/src/views/supplier/list.vue](file:///workspace/chemdoc/frontend/src/views/supplier/list.vue)

**修改内容**:
1. 为 `customer` 表和 `supplier` 表添加 `owner_user_name` 和 `create_user_name` 字段
2. 创建时同时保存用户ID和用户姓名
3. 更新 Model 的 `fillable` 属性，包含新增字段
4. 前端列表直接显示姓名字段（`owner_user_name` 和 `create_user_name`）
5. 修复前端显示的字段名从 `owner_name` 改为 `owner_user_name`

---

### 14:00:xx - 修复新增客户时负责人和创建人为空的问题（第2次修复）
**提交**: `d71984b4`
**修改文件**:
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php)
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)

**修改内容**:
1. 将 CustomerService 的 create 方法从使用批量 create 改为使用 new 对象 + 单个字段赋值 + save() 方法
2. 将 SupplierService 的 create 方法从使用批量 create 改为使用 new 对象 + 单个字段赋值 + save() 方法
3. 这样确保 owner_user_id 和 create_user_id 能够正确保存，不受 $fillable 限制

### 13:50:xx - 修复新增客户时负责人和创建人为空的问题（第1次修复）
**提交**: `18246726`
**修改文件**:
- [backend/app/model/CustomerModel.php](file:///workspace/chemdoc/backend/app/model/CustomerModel.php)
- [backend/app/model/SupplierModel.php](file:///workspace/chemdoc/backend/app/model/SupplierModel.php)

**修改内容**:
1. 在 CustomerModel 中添加 `$fillable` 属性，允许 `owner_user_id` 和 `create_user_id` 等字段批量赋值
2. 在 SupplierModel 中添加 `$fillable` 属性，允许 `owner_user_id` 和 `create_user_id` 等字段批量赋值
3. 确保创建客户和供应商时，创建人和负责人能够正确保存为当前登录用户

### 13:11:16 - 修复 ContactService 中类引用问题
**提交**: `ed0a5858`
**修改文件**:
- [backend/app/service/ContactService.php](file:///workspace/chemdoc/backend/app/service/ContactService.php#L141)
- 修复 `update` 方法中类引用问题，将 `\app\service\Result::success` 统一改为 `Result::success`
- 保持与其他 Service 文件一致的导入方式

### 09:32:11 - 主要分支切换
**提交**: `9e9d7a19`
- 确认 main 分支为主要开发分支
- 之前的修复都在 main 分支上
- 停止使用 trae/solo-agent-5Q8scU 分支

---

## 2026-05-25

### 数据权限隔离修复
**提交**: `168e9cf7`
**修改文件**:
- [backend/app/service/DataScopeService.php](file:///workspace/chemdoc/backend/app/service/DataScopeService.php) - 新增
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php)
- [backend/app/service/OrderService.php](file:///workspace/chemdoc/backend/app/service/OrderService.php)
- [backend/app/service/PurchaseService.php](file:///workspace/chemdoc/backend/app/service/PurchaseService.php)
- [backend/app/service/StatisticsService.php](file:///workspace/chemdoc/backend/app/service/StatisticsService.php)
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)
- [database/migrate_supplier_owner_user.sql](file:///workspace/chemdoc/database/migrate_supplier_owner_user.sql) - 新增
- 前端重新构建

**修改内容**:
1. 添加 DataScopeService 实现数据权限隔离
2. 对客户、订单、供应商、采购、统计模块应用数据权限
3. 添加供应商表 owner_user_id 字段
4. 业务员只能查看自己名下的数据

### 客户删除逻辑修复
**提交**: `f770157e` (之前修复)
**修改文件**:
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php#L285-L328)

**修改内容**:
1. 客户删除前检查是否有关联订单，有订单则禁止删除
2. 客户删除前检查是否有关联联系人，有联系人则提示确认
3. 确认后才执行删除操作，同时删除联系人

### 客户创建人/负责人自动赋值修复
**提交**: `f770157e` (之前修复)
**修改文件**:
- [backend/app/service/CustomerService.php](file:///workspace/chemdoc/backend/app/service/CustomerService.php#L183-L228)

**修改内容**:
1. 创建客户时自动设置 `owner_user_id` 为当前登录用户
2. 创建客户时自动设置 `create_user_id` 为当前登录用户
3. 客户列表显示创建人和负责人信息

### 表单电话号码正则验证修复
**提交**: `910a4408` (之前修复)
**修改文件**:
- [frontend/src/views/customer/contact.vue](file:///workspace/chemdoc/frontend/src/views/customer/contact.vue#L174-L183)

**修改内容**:
1. 手机号验证：`/^1[3-9]\d{9}$/`
2. 座机号码验证：`/^(\d{3,4}-)?\d{7,8}$/`

### 供应商相关修复
**提交**: `f770157e` (之前修复)
**修改文件**:
- [backend/app/service/SupplierService.php](file:///workspace/chemdoc/backend/app/service/SupplierService.php)

**修改内容**:
1. 供应商列表显示创建人和负责人信息
2. 创建供应商时自动设置 `owner_user_id` 为当前登录用户
3. 创建供应商时自动设置 `create_user_id` 为当前登录用户

---

## 2026-05-24

### 菜单权限修复
**提交**: `452da9b8`, `0d907bd8`, `6e8f501a`
**修改内容**:
1. 修复菜单权限显示问题
2. 根据用户角色正确显示菜单
3. 登录时保存 permissions 信息

---

## 重要说明

### 分支管理
- **main 分支**：主要开发分支，所有修改都在此分支
- **trae/solo-agent-5Q8scU 分支**：已废弃，不再使用

### 开发规范
1. 每次修改后必须更新此修改记录
2. 记录修改日期、时间、提交哈希
3. 详细说明修改文件和修改内容
4. 使用绝对路径链接引用文件
5. 保持记录完整、准确、及时更新

### 当前主要功能修复状态
✅ 客户删除逻辑（有订单禁止删除，有联系人需确认）
✅ 客户创建人/负责人自动赋值
✅ 表单电话号码正则验证
✅ 数据权限隔离（业务员只能查看自己名下的数据）
✅ 菜单权限正确显示
✅ 所有服务文件类引用统一
